{
  "name": "email-form-lambda",
  "version": "0.0.1",
  "description": "a serverless lambda hosted in netlify for handling the email contact form on yaniv.online",
  "main": "functions/server.js",
  "scripts": {
    "build": "netlify-lambda build express",
    "start": "nodemon server-local.js"
  },
  "dependencies": {
    "nodemailer": "^6.3.1"
  },
  "devDependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.3",
    "netlify-lambda": "^1.6.3",
    "nodemon": "^1.19.2",
    "serverless-http": "^2.3.0"
  }
}